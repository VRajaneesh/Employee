/**
 * Angular production environment configuration
 */
export const environment = {
  /**
   * Indicates if the application is in production mode.
   * True if in production, false otherwise.
   */
  production: true,

  /**
   * The URL of the backend API for the production environment.
   * This is the endpoint where the application will send requests
   * to interact with the backend services.
   */
  apiUrl: 'https://your-production-api-url',

  // Add other production environment variables here
};
